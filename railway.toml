[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd backend && npm install && cd .. && npm run db:init && npm run start"
healthcheckPath = "/api/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
